version: 1
indices:
  default:
    include:
      - '/**'
    target: /query-index
    properties:
      path:
        select: head > meta[property="og:url"]
      title:
        select: head > title
      description:
        select: head > meta[name="description"]